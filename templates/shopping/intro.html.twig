{% extends 'base.html.twig' %}
{% block body %}
    <div class="container-fluid overflow-hidden text-center">
        <div class="row gx-5">
            <div class="col text-center">
                <div class="p-5">
                    <h3>Ma liste de Courses</h3>
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col text-center">
                <img src="{{ asset('build/images/panier.png') }}" class="img-fluid" alt="Shopping list logo">
            </div>
        </div>
        <div class="row gx-5">
            <div class="col text-center">
                <div class="p-5">
                    <button type="button" class="btn btn-primary"><a href="{{ path('app_login') }}" class="text-light text-decoration-none">Connexion</a></button>
                    <button type="button" class="btn btn-primary"><a href="{{ path('app_register') }}" class="text-light text-decoration-none">S'enregistrer</a></button>
                </div>
            </div>
        </div>
    </div>
{% endblock %}